Colocar aquí `floorplate_gt.json` (mismo schema 0.1.0, provenance=manual), anotado a mano
sobre `original.jpg` con criterio independiente del pipeline. Convención: perímetro = cara
interior de muros. Puede partir de una copia de `outputs/floorplate.json` corregida.
